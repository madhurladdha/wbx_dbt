version: 2

models:
  - name: fct_wbx_mfg_wo_item_agg
    description: mfg item work order item agg 
    columns:
      - name: unique_key
        description: unique id on the table
        tests:
          - unique:
              config:
                severity: warn
                warn_if: ">1"

